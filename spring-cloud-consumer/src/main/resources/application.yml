
spring:
  application:
    name: spring-cloud-consumer

server:
  port: 9001

eureka:
  instance:
    hostname: ${spring.application.name}
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

feign:
  hystrix:
    enabled: true
